{
  "name": "geosteps",
  "version": "0.1.0",
  "private": true,
  "description": "Web-based indoor audio guide for museums: step-detection dead reckoning + map matching + acoustic re-anchoring. No app install, no added hardware.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "server": "tsx src/server/server.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
